"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = (type, title, signer, cover, link, url, duration) => {
    const typeMap = {
        music: 0,
        video: 1
    };
    const data = JSON.stringify({
        b: `=${typeMap[type]}`,
        c: cover.substring(4),
        d: duration,
        n: title,
        o: link.substring(4),
        r: signer,
        s: url.substring(4)
    });
    return `&1${data}`;
};
//# sourceMappingURL=MediaData.js.map