import express from 'express';
export declare class WebServer {
    private app;
    private port;
    private logger;
    constructor(port: number);
    start(): void;
    routeList(): any;
    route(path: string, router: express.Router): void;
    unroute(path: string): void;
}
