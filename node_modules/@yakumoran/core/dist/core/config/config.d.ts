export interface TypeofConfig {
    bot: {
        username: string;
        password: string;
        master_uid: string;
        master_name: string;
        color: string;
        room: string;
        room_password: string;
        port: number;
    };
    database: {
        client: string;
        connection: any;
        useNullAsDefault: boolean;
    };
    plugins: {
        [key: string]: any;
    };
}
export declare class Config {
    private logger;
    private defaultConfig;
    private config;
    constructor(conf?: TypeofConfig);
    getConfig(): TypeofConfig;
    setConfig(config: TypeofConfig): void;
}
