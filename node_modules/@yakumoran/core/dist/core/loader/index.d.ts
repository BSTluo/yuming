import { App } from '../..';
import { Bot } from '../bot';
import { Config } from '../config/config';
import { Plugin } from '../plugin';
export declare const globalPlugins: Map<string, Map<string, Plugin>>;
export declare class PluginLoader {
    private readonly plugins;
    private config;
    private bot;
    private app;
    constructor(bot: Bot, app: App, config: Config);
    load(name: string, plugin?: (app: App) => any): void;
    getPlugins(): Plugin[];
}
